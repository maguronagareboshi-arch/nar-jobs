commit: 枝 s195-ooi-403(origin/master 3a9bb27 の上・⛔push/merge なし・⛔便も DB も画面も 1 字も変えていない)= 新規 docs/ooi_official_403_20260916.md だけ(欠けの表・4 通り×2 か所の status・robots の写し・回避 A/B/C の比較・推し)。⚠ 切り分けを Actions で回すために **別枝 s195-ooi-probe(f9072c9)を 1 本だけ push** した= `.github/workflows/ooi-probe.yml`(**その枝への push でしか起きない**・schedule 無し・枝を消せば二度と走らない)。master は触っていない・merge も deploy もしていない
検品: 欠けは匿名 REST で**読むだけ**(nar_horse_health_events・nar_races)= `source_kind='ooi_official'` **153 行 / 2026-01-12〜2026-09-04 / 全行の created_at が 2026-09-05T16:24:50Z の 1 回きり**= 9/8 の移設後 **0 行**。大井の 2026 開催日 71 日のうち 9/4 までの 66 日中 60 日に行あり(**1 開催日あたり 2.32 件**)。9/4 より後の開催日は 9/14・9/15・9/16・9/17・9/18 の 5 日= **9/16 時点で欠けは 9/14・9/15 の 2 日分(見込み 4〜5 件)**、以後 1 開催日ごとに +2.3 件。切り分けは Actions run **35085669481**(1 分・成功・出口 IP 135.119.39.x)で **4 通りとも 403**(①既定 UA×RSS ②Chrome UA×RSS ③curl×RSS ④既定 UA×HTML 一覧・全部 cloudflare・⛔本文は保存せず status と長さだけ・1 件ずつ 3 秒あけて 1 回)。同じ表を PC でも取り **①②③は 200(15,657 B)・④は PC でも 403**
通信: ±0(便は回していない・DB は読みだけ・DB 書き込み 0)。先方への要求は **Actions 4 本 + PC 7 本の合計 11 本を 1 回ずつ**だけ
回帰: なし= `.github/workflows/horse-health.yml`・`cloud/health_org.py`・`cloud/health_org_ooi.py` は無傷。s195-ooi-probe の yml も**他の便に一切かからない**(on: push の branches で自分の枝に限定・paths でその 1 ファイルに限定・permissions は contents: read・secrets を使わない)
変えた点: (1) 設計の 4 通りに **PC 側の同じ 4 通り+3 通り(Chrome UA×HTML・Chrome UA×トップ・UA を付けない Python×RSS)を足した**= Actions だけでは「UA か IP か」を切り分けられないため。おかげで **UA ではなく IP** と確定し、ついでに **`Python-urllib/3.12` の既定 UA は PC でも 403**(= 便が `nar-jobs-health/1.0` を名乗っているのは正しい)が分かった。(2) 設計の ④「HTML 一覧に替える」は **回避先にならない**と判明= HTML は **PC からでも 403** で中身が `<title>Just a moment...</title>`= **ボット判定の関門**。⛔突破する仕組みは作らない(関門の回避は禁止)ので、A/B/C の表から外した
⚠: **(1) 失うのは「大井だけの 22%」だけではない**= 重なり 120/153(78%)・大井だけ 33/153(22%・競走中止 14/競走除外 9/出走取消 5/競走後の診断 5)だが、**括弧の診断名は 大井 96/153(63%)に対し NAR PDF は 0/128**・**「※予後不良」は 大井 7 に対し PDF 0**。重なる 120 行のうち **86 行は大井だけが診断名を持つ**= §193c の疾病別コラムの材料は PDF で代えがきかない。**(2) A(当サイトの Cloudflare から取る)は未検証**= 先方も Cloudflare なので出口 IP も同じボット判定を受ける見込みがある。**一時の function を 1 本置いて status を見るだけで決まるが deploy が要る= Fable の役**。**(3) A でも B でも「見張り」が前提**= 今回 8 日気づかなかったのは段が `if: always()` で **0 件でも exit 0・便は緑**だから(run 35064946260 で実測= `documents=0 … errors=16`、同じ便の kochi 7 件・saga 4 件は正常)。`ooi_official` の最新 race_date が大井の直近開催日から 2 日以上遅れたら赤、を nar-watchdog に 1 本足す必要がある。**(4) 利用規約のページは写せなかった**= `/policy/` `/terms/` `/sitepolicy/` `/privacy/` は PC からも 403(HTML は全部関門の内側)= 人がブラウザで読むしかない。robots は写せた(`User-agent: *` は `Disallow: /wp-admin/` だけで **`/news/feed/` は禁じていない**)
要判断: **A / B / C のどれか**。推しは「**まず A を 1 回試す**(function 1 本・status だけ・すぐ消す)→ 通れば A(失う物なし・通信は今の 16 本より減って 8 本)/ 通らなければ **C**(段を外し『競走中止の診断名は持っていない』を画面に 1 行)」。**B は『ユーザーは PC を閉じている前提』に反するので推さない**。補足 **D= 先方に問い合わせて通してもらう**(RSS は機械に開いており robots も禁じていない= まっとうに直す唯一の道)も、投げるかどうかはユーザー判断。⛔UA 偽装・IP 変え・関門の突破はどの案にも入れない。あわせて **枝 s195-ooi-probe をいつ消すか**
