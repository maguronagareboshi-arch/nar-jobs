commit: 枝 s195b-ooi-relay(nar-jobs・**origin/master 58f79f3**(= §195 段 1 の doc が既に master に入っていた)の上・⛔push/merge なし・⛔便は 1 回も回していない・⛔DB に 1 行も書いていない)= `cloud/health_org_ooi.py` の入口だけ差し替え(定数 `FEED` を廃止 → 新規 **`FEED_BASE_DEFAULT = "https://nar.yukochi.com/feed/ooi"`** と純関数 **`feed_base()`**(環境変数 `OOI_FEED_BASE`・空文字と空白だけは未設定と同じ)・**`feed_url(page, base=None)`**(根に `?` があれば `&` で継ぐ)。頁送りの 1 行 `FEED + "&paged=%d"` → `feed_url(page)`。先方の URL は**説明(コメント)にだけ**残した。使わなくなった `import urllib.parse` を外し `import os` を足した)・`.github/workflows/nar-watchdog.yml` に **job `ooi-official`** を 1 本追加(kochi-results と同じ形・08:40 JST と手押しだけ・読むだけ)・新規 tests/test_ooi_feed_base.py 7 項
検品: `unittest discover -p "test_*.py"` **73/73 OK**(66 → +7)・paper_match_test 17/17 OK・py_compile 通過・git diff --check 通過・禁止語 0。新規 7 項= ①既定が中継 ②環境変数の上書き(query 付きの根も `&` で継ぐ)③空・空白の環境変数は既定に戻る ④引数は環境変数より強い ⑤8 本とも別 URL ⑥**`list_documents` が本当に `feed_url` を通る**(偽の fetch で 16 本= 8 頁 × 引き直し 1 回・⛔`FEED` 定数が残っていないこと)⑦**読み方が無傷**(SOURCE_KIND・PARSER_VERSION・TRACK・`parse()` の 1 件)。**実物での通し(⛔DB には書かない)**= `OOI_FEED_BASE` で先方を指し PC から **1 本だけ**引いて `list_documents`+`parse`= **文書 4 件(9/3・9/4・9/14・9/15)/ 行 11 件**が今までどおり読めた(= **欠けていた 9/14・9/15 が入る**ことを確認)。**watchdog の中身も手元で実走**(yml から抜き出して匿名 REST で)= `大井の直近の開催日 2026-09-16(直近 60 日で 19 日)/ ooi_official の最新 race_date 2026-09-04 / 遅れ 12 日 / 取れていない開催日 3 日 [9/16, 9/15, 9/14]` → **exit 1(= 今は正しく赤)**。⛔手順名に「: 」は無い(yml を読み込んで 4 job 全部の手順名を機械で確認)
通信: **±0**(便は回していない・DB は読みだけ・書き込み 0)。先方への要求はこの検品で **PC から 1 本**だけ。運用に入った後は **1 日 8 本**(中継の棚が 10 分効くので、便が 2 回読んでも 8 本。**今までの 16 本から半分**)。watchdog は 1 日 1 回・REST 2 本
回帰: `_page`・`list_documents` の頁送りの作法(1 回だけ引き直す・古い頁で break)・`parse`・`SECTIONS`・`_IDENT`・`_LEAD` は **1 字も変えていない**。`health_org.py` 本体・`health_org_kochi.py`・`health_org_saga.py`・`horse-health.yml`・DB の器は無傷。watchdog の既存 3 job(freshness・odds-live・kochi-results)は 1 字も動かしていない
変えた点: (1) watchdog の job は設計の「anon REST 読みだけ」に対し、**同じ yml の他の 3 job と同じ `secrets.NAR_SUPABASE_URL` / `NAR_SUPABASE_SERVICE_KEY` を読み取りだけに使った**= 匿名鍵を yml に直書きすると鍵の正本が 2 つになるため(やっていることは select 2 本だけ)。(2) 遅れの判定に加えて **`取れていない開催日` の一覧を print する**ようにした(赤になったとき「何日ぶん入っていないか」がログで分かる)。(3) 使わなくなった `import urllib.parse` を外した(先方の URL を組み立てなくなったため)
⚠: **(1) この枝だけでは直らない**= 中継(viewer 枝 s195b-ooi-relay)を **先に deploy** してからでないと、便は `https://nar.yukochi.com/feed/ooi` を叩いて 404 の殻を受け取る。順は設計どおり **a deploy → curl で 200/400 → b merge → horse-health 手押し(source=nar・dry_run=false)**。**(2) watchdog は今 赤**(実測 exit 1)= b を入れて便を 1 回回すまで鳴り続ける。先に watchdog だけ merge すると毎朝 1 通赤が来るので、**b と一緒に入れるか、便を回した直後に入れる**のが良い。**(3) 誤って鳴る可能性がわずかにある**= 大井が開催しても事故ゼロの日は行が立たない(実測 66 日中 6 日= 9%)。2 日続けてゼロだと遅れ 2 日で赤になる(確率およそ 1%)。設計どおり「2 日」にしてあるので、うるさければ 3 日に緩める。**(4) viewer 側にも同じ adapter の古い写しがある**(`s77-w1/cloud/health_org_ooi.py`= docstring の 1 語だけ違う 9/8 移設前の物)。**誰も回していない**(nar-viewer に workflow は 1 本も無い)が、`s77-w1/tests/health_org_ooi_test.py` はそちらを見ているので、揃えるかどうかは要判断= 今回は設計の範囲外なので触っていない
要判断: (1) **⚠(4) の viewer 側の写しを揃える / 消す / このまま置く**のどれか (2) watchdog の遅れの線を **2 日のままにするか 3 日にするか**(⚠(3))
