commit: 枝 s202-ai-last(nar-jobs・origin/master 6e01bcb の上・設計 docs/opus_s202_ai_last_loop_20260917.md・⛔push/merge/deploy なし・⛔本番 DB への書き手は増やしていない)= **A 新規 cloud/ai_last_today.py**(公式の当日 ZIP を refresh.py と同じ部品(download_url/download_archive/normalize_archive)で 1 回取り、`today/official_horses.tsv`(体重のある走だけ・増減の空は \N)と `today/official_races.tsv` を書く/取りに行く条件= 手元(ジョブ内 Postgres・pg8000)の当日で「発走まで 15〜90 分・取消除外でない走に体重の空がある」レースがあるときだけ/無ければ「official today: skip(待ちのレース無し)」/失敗(ZIP でない・タイムアウト 60 秒・手元の表が読めない)はログして 0= 周を止めない/取らない回も空の TSV を書く= yml の \copy がそのまま通る/ログ「official today: 体重あり N 走・DB に無かった M 走(レース名)・待ち K レース(…)」)・**B .github/workflows/nar-ai-last.yml**(周 15 分→**5 分**(SLEEP 300)・手順名「predict last every 5 minutes」・**on.schedule の 2 本を外した**(起動は pg_cron の dispatch だけ・WRITE/ONCE は inputs から)・周の順= 本番 \copy → 地元差し替え → **ai_last_today.py → psql で temp 表 o_h/o_r に \copy → `update nar_runs … where u.body_weight is null` と `update nar_races … where o.going<>'' and coalesce(r.going,'')=''`(地元だけ・失敗しても周は続ける)** → refresh_nar_ai_feat → predict/write・install に requests(公式の部品が使う)・冒頭のコメントを更新)・**C 新規 tests/test_ai_last_today.py 3 項(60 行)**
検品: `unittest discover -p "test_*.py"` **95 本 OK**・py_compile 通過・yml は yaml.safe_load で読める(on= workflow_dispatch だけ)・手順名に「: 」なし・周の bash を取り出して `bash -n` 通過。**`python -c` の部品確認= 公式の当日 ZIP(9/17・53,872 bytes・0.6 秒)→ normalize= 48 レース・486 走(名古屋・園田・大井・門別)→ overlay_rows= 体重あり 483 走(TSV の 1 行目「大井 2026-09-17 1 1 505 \N」)・race_rows 48・waiting_races(19:30 として全頭を体重なし扱い)= 名古屋/大井/門別の 11R・12R(発走まで 15〜90 分)**。手元の Postgres が無いとき= 「official today: skip(手元の表が読めない InterfaceError)」・空の TSV 2 つ・rc 0。**手押しの run は未実施(push 後に Fable)= run の URL と Actions 上の「official today」の行はまだ無い**
通信: 公式= 待ちのレースがある周だけ 1 本(ZIP ≈54KB・0.6 秒)= 最大 12 本/時(5 分おき)・待ちが無い周は 0。本番 DB= 今までどおり周ごとの当日 2 表の \copy(周が 15→5 分で 3 倍= 1 本あたり当日分だけで小さい)と --write の REST upsert だけ。検品で公式へ 3 本(当日 ZIP の読み)・本番 REST の読み 1 本(発走時刻の書き方)
回帰: base_v1.py(印の規則・締切 15 分)・pipeline/sql・refresh.py・本番のトリガー・pg_cron は触っていない。便の頭の全量の写し・模型の取得・終わりの時刻(14:55/20:55)・timeout 358 は不変
変えた点: (1) **発走時刻は「1550」(コロンなし)で読む**= 本番 nar_races と公式 CSV の post_time は 4 桁(9/17 の 48 レース全部)で、「15:50」だけを読む形だと待ちのレースが永久に 0 になっていた(実データで発見)→ 「1550」と「15:50」の両方を読む。(2) 待ちの判定と「DB に無かった」は、base_v1 の予測と同じく finish_note が空の走だけで数える(取消・除外の走は体重が来ないので待ち続けない)。(3) `--now` を足した(検算用)
⚠: **周が 5 分になり本番への当日 2 表の \copy が 1 便あたり ≈72 回(今まで ≈24 回)**= 1 回は当日分だけ(数百行)で軽いが、本番の負荷と Actions の分数(2 便 × 最長 358 分は不変)を Fable が見る
要判断: (1) 手押し `gh workflow run nar-ai-last.yml -f write=no -f once=yes` と `gh run list -w nar-ai-last.yml` の確認(Fable・push 後) (2) 待ちの窓 15〜90 分(公式の発表は発走の 45〜60 分前)で足りるか
