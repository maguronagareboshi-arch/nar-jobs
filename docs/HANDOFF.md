commit: 枝 s193c-b-auction-groups(origin/master 31ac30f の上・⛔push/merge なし・⛔便は回していない・⛔DB に 1 行も書いていない)= cloud/horse_health.py に**オークション出典だけ**の語表を新設(新規 AUCTION_GROUP_RULES= 13 器 bone_joint・tendon_ligament・hoof・muscle_back・respiratory・epistaxis・cardiac・digestive・eye・skin_wound・fever_infection・symptom・castration/新規 AUCTION_NEG_TAIL= 病名のすぐ後ろが否定の形のときだけ落とす/新規 AUCTION_MAYBE_TAIL= 「〜になるかもしれない」は事象にしない/新規 AUCTION_PLAIN_NEG・AUCTION_NOTICE・AUCTION_EXAM・AUCTION_IGNORE・RACING_ABILITY_LOST_RE/新規 純関数 _auction_terms= 文 → [(分類, 当たった語)])・`_auction_details` は `_groups(detail)` の呼び出しだけを `_auction_terms(detail)` に差し替え・束ねた 1 事象に **condition_term(当たった語)と racing_ability_lost(競走能力喪失の印)**を持たせて RPC の payload に 2 列追加・PARSER_VERSION を health-v1.1.1 → **health-v1.2.0**(1 記述 1 件の鍵の作り方は変えない)・新規 tests/test_auction_groups.py 9 項。⛔**主催者出典の GROUP_RULES・_groups・NEGATIVE_HEALTH_RE・RIDER_ONLY_RE は 1 字も変えていない**(消した 17 行は全部オークションの経路と型の注記だけ)
検品: `unittest discover -p "test_*.py"` **66/66 OK**(新規 9 項= 13 分類×代表文 2・打ち消し 6・「発症したが今は問題ない」は事象 2・装蹄/ワクチンは出ない 2・検査だけ/注意書きは出ない・症状だけは他が無いときだけ・印と語・1 文 2 分類・主催者の語表が変わっていないこと)・paper_match_test 17/17 OK・py_compile 通過・git diff --check 通過・禁止語 0。**精度の目視= 13 分類ごとに無作為 30 文(合計 390 文)を読み、誤当たりは全分類 1/30 以下**(bone_joint 0・tendon 0・hoof 0・skin 0・respiratory 0〜1・epistaxis 0・cardiac 1・digestive 0・eye 0・skin 0・fever 0〜1・muscle 0〜1・symptom 1・castration 1)。手元の原本 414,310 文に当てた結果= 事象の文 23,650・打ち消しで捨てた 3,674・検査だけ 7,215・装蹄など 445・判定待ち 1,413 文。束ねた後の事象 **14,454 件 / 9,611 出品**(bone_joint 4,587・skin_wound 2,638・tendon 1,541・castration 1,298・symptom 1,205・respiratory 857・hoof 795・epistaxis 731・fever 285・digestive 175・eye 137・cardiac 104・muscle_back 94・unspecified 7)・競走能力喪失の印 66 件。**判定待ちの出品は 窓(2026-01-01〜09-15)で 390 → 100**(設計の「本当に不明 約 1,200 文分より下」を満たす)
通信: ±0(便を回していない・DB 読み書き 0・原本は手元のリポジトリの中)
回帰: 主催者出典(取消・除外・中止)の経路は 1 行も通らない= `_groups`・`GROUP_RULES`・`KNOWN_HEALTH_RE`・`NEGATIVE_HEALTH_RE`・`RIDER_ONLY_RE` はそのまま(テストで 5 分類の名前と 3 つの当たり方を見張っている)。`cloud/health_org.py` が import している `_groups` も無傷。§193/§193b で足した `--dump-review`・`--dump-all`・`line_status` はそのまま動く(line_status は主催者と同じ `_groups` を見るので、オークションの新しい分類は出さない= a0 の表のとおり)
変えた点: 精度の線(1/30)を満たすため、設計の語表から **3 語を落とし 5 語を狭めた**= ①「消化不良」を落とした(19 文中 11 文が「消化不良の競馬」= 競馬の言い回し)②「ショックウェーブ」「焼烙」を落とした(処置の名前で、背腰の疲れ・骨瘤の処置に当たる)③「輸送熱(?!予防)」(「輸送熱予防の注射」= 費用の案内)④「両眼(?!管)」(「両眼管内側の骨瘤」)⑤「去勢(?!すれ|してみ|するの|しておけ|した方|したほう|も考え|を考え|も検討|を検討)」(「去勢も考えました」= まだしていない)。打ち消しには設計の形に加えて「<病名>を(これまで)発症したことはありません」「〜の診断ではありません」「〜は見られず」を足した(目視で出た実例)
⚠: **(1) この枝だけでは本番に出せない**= `condition_term`・`racing_ability_lost` の 2 列と、13 の新しい `condition_group` の値は **a の SQL(表の CHECK・RPC の列)が先**。今のままの RPC は知らない鍵を黙って捨てるので、2 列は書かれず、新しい値は表の CHECK で insert が弾かれる。**(2) 画面の門番**= viewer `js/data.js:4445 HEALTH_GROUPS` に 13 値を足さないと、DB に入っても画面から黙って消える(a0 の ⚠(2)・c の担当)。**(3) 分類ごとの数は設計の ±15% に 6/13 しか入らない**= 6 つは目安より多い(bone +13.7%は内側/skin +17.1・tendon +17.8・epistaxis +17.3・symptom +32.5・respiratory +36.6・eye +28.3)= §193b の目安は「1 文 1 分類・粗い語表」で数えた値なので、語を足したぶん増えるのは想定どおり。1 つだけ少ないのが **muscle_back -70.8%**(ショックウェーブ・焼烙を落とし「背腰に疲れ」を事象にしないため)。⛔数を合わせるために語を戻すと精度が落ちるので戻していない= 目安を引き直すか muscle_back をこのまま通すかは Fable 判断。**(4) PARSER_VERSION を上げたので、d の `--reparse` まで既存のオークション行は古い版のまま**(reparse_waiting に出る)
要判断: (1) 数の目安(±15%)を新しい語表で引き直すか、muscle_back の -70.8% を承知で通すか (2) `digestive` から落とした「消化不良」と `muscle_back` から落とした「ショックウェーブ・焼烙」を、画面に出さない別の形(処置だけ)で拾うか捨てるか
