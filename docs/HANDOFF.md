commit: d7977cb 枝 s237-weight-minus(origin/master 72c4b71 から)。設計 viewer-master docs/opus_s237_weight_minus_20260920.md A・B。_number に allow_negative・新規 cloud/fix_weight_change.py と tests。push と --apply は OK 待ち
検品(手元): 増減だけ負を通す(-4・全角－4・数学−4・ハイフン‐4 が −4)。ほかの列(上がり3F・賞金・距離)は今までどおり負なら None。±0・空・-・取消 の扱いは不変。unittest 116 本中 6 本が新規で緑
通信: 本番への書き込み 0(ドライランだけ)。読みは月ごとに 1000 行ずつ= 47 か月・約 620 本。⛔全件数えは 500 が返るので使わない(期間を区切って数えた)
回帰: 既存の unittest は 110 本のうち 2 本が失敗するが、これは私の変更前(origin/master)でも同じ= test_ooi_raw_read の 2 本(nar_fetch_raw の頁が無い)。yml・他の便・生 ZIP・ビューアは触っていない
ドライランの内訳: 生 ZIP 131 本 → 増減が負の行 **256,052**。全部 nar_runs に行があり(送らない 0)、**全部が今 null**。年別= 2022 10,586 / 2023 66,592 / 2024 66,211 / 2025 65,801 / 2026 46,862。場別は 園田 26,552〜姫路 6,245(表は報告に)
⚠ 接続先: 他場 の .env は**古いプロジェクト**を指していた(照合が全月 0 行= 送信 0 で止まった)。nar の本番は pipeline の .env.nar だが手元に無いため、内訳は読み取り専用の鍵で出した= **--apply は鍵を持つ側で**
要判断: (a) --apply を誰が流すか(鍵の所在) (b) 一度に 256,052 行(500 行ずつ 513 本の upsert)でよいか・年ごとに分けるか (c) push の可否
