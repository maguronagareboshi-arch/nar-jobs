commit: 枝 s196b-hist(nar-jobs・origin/master 2a2e83b の上・設計 docs/opus_s196b2_nankan_hist_20260917.md・⛔push/merge/deploy なし・⛔本番へは 1 行も書いていない(初回 --all は Fable)・⛔Actions yml は触っていない)= **A 新規 cloud/nankan_hist.py**(420 行・第 1 段の逆算を移した= 公式の表・`earned_table`(オープン=A1・３歳新馬=3 歳の表・混合=上のクラス)・`meetings`(同じ場で間 4 日以内=同じ開催・4 月で回を数え直す)/`run_points`= nar_races.nankan.pts があればそれ・無ければ表・同着は等分・取消除外/中止=0・着順なしと S 格(pts なし)は None/`kaku_of_race`= 単一クラスで選抜・選定・オープン・重賞でないときだけ格/`hist_rows`= 開催最終日ごとに 1 行・反映は「開催最終日 < asof」・いまの値から引く(未反映の開催は足す)・**既にある official の行は書き直さず、それより古い開催はいちばん近いその行から引く**・決められない走より古い開催は points null・最後に反映された開催で後ろに反映済みの走が無ければ src=official・kaku は race/carry(無ければ null)・遠征は賞金÷1万を端数のまま足し引きし、列が整数なので書くときだけ四捨五入/材料= REST(nar_races は月ごと・馬名 40 件ずつ・同着は対象馬が 5 着以内のレースだけ引く)か `--from-csv`(第 1 段の \copy CSV)/`--days`(既定 3)・`--all`・`--shard i/n`・`--out`・`--today`/upsert on_conflict code,meet_end)・**B cloud/nankan_points.py に ④ 結果ページの段**(`parse_result`= 番組ポイント 5 つ・重賞の格(SIII/JpnIII など)・日付と場が違うページは捨てる/`patch_nankan`= nar_races を 1 レースずつ PATCH(他の列は触らない)/`fetch_results`= raceid は ③の馬ページのリンク(本物)→ 開催カレンダー → DB の日程(回 ±1 も試す)/窓の中で走り終えて nankan.pts が無いレースだけ・既定上限 150 本/`--results-dates A..B [--results-kind 重賞]`= 過去分だけ(馬ページはしない)/`--no-results`。③の表の書き込みは④の前に済ませる= ④で失敗しても③は残る)・**D tests/test_nankan_hist.py 7 項(79 行)**
検品: `unittest discover -p "test_*.py"` **全部 OK**(既存+7)・py_compile 通過。**合格 1= 第 1 段の 50 頭(seed 196)で、表の points が data/recon/recon_all.csv(各開催の最後の走の points_after)と 893 行すべて一致・kaku_ran(race)466 行が結果ページの格と 466 行すべて一致**(carry 215・null 212 は数えない)。REST で作った行(--days 3・656 頭)と CSV で作った行は共通 9,854 行で違い 0。結果ページの読み取り= 第 1 段のキャッシュ 893 本すべて読めた(日付・場の確認つき・SIII 5・JpnIII 2)。**合格 2 の見込み(CSV で --all を流した結果・本番の --all は Fable)= 3,339 頭・45,499 行(1 頭最大 53 行)・points null 3,254 行(7.2%)・official 3,233 行(official の無い 106 頭のうち 95 頭は最後の開催がまだ反映前)・points<0 の行 9(8 頭・全部 遠征あり)**
通信: nankankeiba= 試しに **7 本**(過去の S 格重賞 1 本を 2 回・昨日の大井 5 本・どれも書き込みなし)。本番 DB= 読みだけ(閲覧用の anon キーで REST。書き込み 0)。運用に入った後= 日次の ④ は 1 日 ≈ 40 本 ≈ 60〜80 秒(上限 150)・nankan_hist の日次は REST の読み ≈ 40 本+upsert(走った馬 ≈ 150 頭 × 最大 53 行・既にある official の行は書かない)。初回の埋め= `--results-dates 2024-01-01..2026-09-16 --results-kind 重賞`(S 格重賞 114 本 ≈ 3 分)→ `nankan_hist.py --apply --all`(REST の読み ≈ 250 本・upsert 45,499 行 = 91 本)
回帰: nankan_points.py の ①〜③(馬ページの流れ・表 nar_nankan_points の列と取り直し規則)は不変(④を足しただけ・③の書き込みは④の前)。tools/nankan_recon.py は第 1 段の記録として残した(式は nankan_hist.py と同じ= 合格 1 で確認)。**C= yml は触っていない。nar-refresh.yml の朝の便で `python cloud/nankan_points.py --apply --days 2 --max-horses 400` の直後に足す 1 行= `python cloud/nankan_hist.py --apply --days 3`**(手順名に「: 」を書かない)
変えた点: (1) 設計の「official= 主催者の値がその開催の後の最初の値」は、**後ろに反映済みの遠征があるときは recon** にした(主催者の値に遠征の賞金が入っている= その開催の直後の値ではない)。(2) **既にある official の行を起点**にした(日次で書き直さない・古い開催はいちばん近いその行から引く= 設計の「逆算のずれはここで止まる」を式にした)。(3) 遠征の端数= 設計は「切らない」だが列が integer= **足し引きは端数のまま・書くときだけ四捨五入**。(4) `--from-csv` と `--today` を足した(初回の埋めを本番に重い読みをかけずに流せる・第 1 段の \copy と同じ材料)。(5) 1 頭の行数は最大 53(設計の見込み ≤ 40 より多い)
⚠: **points null の行 3,254(7.2%)の元になった走= S 格の重賞で pts なし 341 走・DB に着順が無い 275 走・遠征先の地方交流重賞 13 走**(S 格は ④ の --results-dates で nankan.pts が入れば解ける・着順の欠けは工事 B)。points<0 の 9 行(8 頭)は遠征のある馬で、値のまま(null にしていない)
要判断: (1) **kaku_ran が空の行が 16,795 行(37%)**= 2・3 歳のレース(条件に格が無い)で、前の開催の格も無い馬。画面(工事 C)で「格なし(2・3 歳)」と「決められない」を言い分けるか (2) carry 9,756 行(21%)= 選抜・混合・オープンで前の開催の格を引き継いだ行。半期の切替をまたぐ carry は実際の格とずれうる(第 1 段で昇級の遅れ・半期切替の直後が 13 走)= 画面で carry を印で分けるか (3) official の行は馬ごとに 1 行から始まり、日次で増える= 画面で「主催者の値」と「逆算」を見分けて出すか
