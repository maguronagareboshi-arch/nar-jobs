commit: 枝 s196b-recon(nar-jobs・origin/master 5047ef4 の上・⛔push/merge/deploy なし・⛔DB に 1 行も書いていない・⛔Actions yml と cloud/*.py は触っていない)= 新規 **docs/s196b_rules.md**(公式 Q&A を cp932 で 1 回だけ取り、タグを外した本文から①〜④を行番号付きで引用・無いものは「原文に無い」= ①昇級は反映時点(L227「開催最終日の全レース終了後」)と半期ごとの基準(L492)だけで「開催ごとに昇級」は原文に無い ②降級の時点は原文に無い ③転入馬・移行時の換算は原文に無い(⚠記憶メモの「旧賞金÷1万」は今の原文に無い)・3・4 歳の格付時期の表(3 歳 2 月 A1 級…10 月 C2 級・4 歳 1 月 C3 級)・4 月以降の 3 歳 C3 は 20 以上(L556)④同着(L222)・失格/着順変更(L234)は原文あり・取消/中止は原文に無い)・**docs/s196b_recon.md**(結果)・**tools/**(`nankan_recon_dump.sh`= 本番から psql 1 接続で 4 表を `\copy`/`nankan_recon.py`= 逆算と 50 頭の検算/`nankan_recon_report.py`= 不一致を原因で分ける/`nankan_http.py`= 1.8 秒間隔・cp932・キャッシュ/`nankan_rules_fetch.py`)・tests/test_nankan_recon.py 5 項・`.gitignore` に `data/`(キャッシュと \copy の CSV は git 外)
検品: `unittest discover -p "test_*.py"` **80+5 OK**(既存 80 はそのまま緑)・git diff --check 通過。**着内ポイント= 50 頭 893 走中 889 走が公式の結果ページ「番組ポイント」と一致**(直した 2 点= 「◯◯オープン」の特別は A1 の表・「３歳新馬」は 3 歳の表。混合クラスは上のクラスの表で不一致 0。残る 4 走= S 格の重賞 2・DB に着順が無い 2)。**格= 公式の点で逆算して 759 走中 741 走(97.6%)一致**(DB の点では 732)。不一致 18 走は原因で分けた= 昇級の遅れ 8・半期切替の直後 5・選抜/選定馬 3・境界の数点 1・その他 1(遠征・同着・転入直後 0)。R1(直前の全走)と R2(前々開催まで)は 50 頭で差 0 = 判別できず・A/B(降級の時点)はこの計算では必ず同じ(ポイントは減らない・基準は半期内一定・馬齢は 1/1 に上がる)。**全頭(2024 年以降)= 3,339 頭中 2,935 頭(87.9%)が全部の走で復元できた・points_before<0 は 20 頭(全部 遠征あり)**
通信: **nankankeiba へ 1,163 本・約 37 分**(1.8 秒間隔・cp932。内訳= Q&A と試し読み 4/検算 1 回目 873 本 28 分(うち 404 が 283 本= DB から組んだ raceid のずれ)/取り直し 282 本 約 8.5 分/3 回目 4 本(9/17・9/18 のまだ無い結果で 404))。本番 DB= **読みだけ**(`\copy` 1 接続 10 秒・形を見る軽い SELECT 5 本)
回帰: 既存の便・DB・viewer は不変(新規ファイルだけ+.gitignore 1 行)。`cloud/nankan_points.py` は UA 定数を読むだけ(import)で動作は変えていない
変えた点: (1) **反映の時点を「開催の最終日 < 基準日」で数えた**(設計書の「race_date >= asof を引く」のままだと、2024 年以降にマイナスが 181 頭= 新馬の初走がちょうど 1 走ぶんマイナス → 原文 L227 どおりに直して 44 頭 → 表の直しを入れて 20 頭)。(2) 過去の出馬表 `/uma_shosai/` には**馬ごとの当時の格が無かった**(470KB)ので、**結果ページ `/result/`(97KB・レースの格と番組ポイントがある)**で突き合わせた= 当時の格はレースの格(単一クラス)・混合クラスは「どれかに入れば一致」。(3) raceid は設計書の calendar 経由(直近 2 か月しか引けない)ではなく、**DB の開催日程(同じ場で間 4 日以内=同じ開催・4 月始まりの年度で回を数える)**から組み、**馬ページのリンクの本物の raceid を優先**した(2024 年以降 857 一致・船橋で 42 不一致)。(4) 2024-01-01 より前は点数表が当てはまらない(移行時の換算は原文に無い)ので、復元とマイナスの数から外した
⚠: **復元できない馬が 404 / 3,339 頭(12.1%)**= 止めた理由 DB に着順の無い走 250 頭(nar_runs に結果の欠けが 275 走)・S 格の重賞 152 頭(DB の名前に格が無い)・遠征先の地方交流重賞 2 頭。止まった走より新しい側は復元できる。ほかに 抹消などで いまの値が無い 420 頭は対象外。遠征のある馬 20 頭はマイナス(端数の扱いと他地区在籍期間は原文に無い)
要判断: (1) **ポイントの履歴は「開催の最終日」単位**で持つ(走った日単位だと 1 走ずれる)(2) **格の履歴は逆算の点からの計算だけでは 2〜3% ずれる**(昇級の遅れ・半期切替の直後に前の格で数週走る)= 実際の格は単一クラスのレースの条件から取り、混合・選抜は前後で補うか「不明」にするか (3) **nar_races に「そのレースの番組ポイント 5 つ」と「重賞の格」を持たせるか**(主催者の結果ページにある・2024 年以降の S 格重賞は 114 本で S 格の止め 152 頭の多くが直る)(4) raceid を取得元から表に持つか(DB の日程から組むと船橋でずれる)(5) nar_runs の着順の欠け 275 走を取り直すか
