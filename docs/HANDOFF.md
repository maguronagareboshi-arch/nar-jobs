commit: 枝 s192b-paper-404(origin/master 80172cc= §192 段 1 merge 済み の上・⛔push/merge なし・⛔便は回していない)= cloud/paper_first3f.py(新規 例外 Gone(Quiet)= 一覧に名前はあるが本体が無い/http_get は HTTPError の番号を例外に持たせる(q.status= 字「HTTP %d」は今までと同じ)/one_pdf は **PDF 本体の取得が 404 のときだけ** Gone に変える(一覧の読み取り・5xx・通信失敗・読み取り失敗は今までどおり Quiet= 失敗)/main は Gone を「<日付> <N>R 一覧にあるが本体なし(HTTP 404)」の 1 行 log で数え、fails に足さない= rc 0 のまま/集計行を「…・失敗 %d 本・本体なし %d 本」に足した)・tests/paper_match_test.py(新規 Missing 2 項= 本体 404 で rc 0・500 で rc 1)
検品: `py -3.12 -X utf8 -m unittest tests.paper_match_test` **17/17 OK**(新規 2 項= ①404: rc 0・log に「2026-09-01 12R 一覧にあるが本体なし(HTTP 404)」と「失敗 0 本・本体なし 1 本」・log に「.pdf」の字なし ②500: rc 1・log に「2026-09-01 12R 失敗 HTTP 500」と「失敗 1 本・本体なし 0 本」)。既存 `unittest discover -p "test_*.py"` **57/57 OK**。py_compile 通過・git diff --check 通過・禁止語 0・コードに URL の字なし
通信: ±0(便は 1 回も回していない。段 1 で実際に 404 だった 5 本= 9/1 12R・5/04 1R 2R・4/19 8R 9R)
回帰: .github・cloud/refresh.py・cloud/horse_health.py・pipeline は触っていない。404 以外の落ち方(5xx・timeout・PDF が読めない・DB 失敗)は数え方も終了コードも今までどおり。集計行の字が「失敗 N 本」→「失敗 N 本・本体なし N 本」に伸びたので、この行を見張る道具があれば当て直しが要る(手元の grep は「失敗 [1-9]」で拾っており影響なし)
変えた点: 設計の「1 行 log(件数だけ)」は、既存の失敗行と同じく **日付とレース番号を付けた 1 行**にした(どの回の残りかすか分からないと追えないため。⛔ファイル名・URL は出さない= モジュールの ⛔規則「出してよいのは 日付・レース番号・件数・保留の理由・status 番号だけ」の内側)。404 の見分けは字の突き合わせでなく **例外が持つ status 番号**で行う(「DB HTTP 404」など別の 404 を巻き込まないため)
⚠: **本物の 404 での確かめは push が要る**(便は master から回るので、この枝のままでは Actions で試せない)= 実機の判定「朝便の既定窓に 9/1 12R が入っても success」は Fable の push 後に 1 回 手押し(dates=2026-09-01)で確かめてほしい。手元では偽の http_get を差し込んだテスト 2 項までが確かめた範囲
要判断: なし(設計どおり。404 以外は今までどおり失敗のまま)
