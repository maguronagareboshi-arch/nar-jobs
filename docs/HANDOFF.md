commit: 枝 s193c-a0-groups-grep(origin/master 31ac30f= §193・§193b merge 済み の上・⛔push/merge なし)= **コードは 1 字も変えていない**(§193c a0 は grep だけ)。変えたのは この docs/HANDOFF.md だけ。6 値('disease','musculoskeletal','epistaxis','cardiac','accident','unspecified')の一覧が埋まっている場所は **18 か所 / 3 つの木**。オークションの経路に効くのは **6 か所だけ**(下の表の ★)。行番号は nar-jobs 31ac30f / viewer(s77-w1)0958694 のもの
検品: 3 つの木を 6 語それぞれで grep(nar-jobs= py・sql・js・yml/ viewer s77-w1= js・mjs・html・css・sql/ pipeline/sql は**両方**にある)。⚠ 設計書が指す `pipeline/sql/horse_health_organizers_20260904.sql` は **nar-jobs に無く viewer 側にある**(nar-jobs の pipeline/sql は 17 本だけの部分写し)。表= ①★viewer pipeline/sql/horse_health_20260903.sql:54-55= 表 nar_horse_health の CHECK(**新しい値は insert が弾かれる= いちばん効く**)②★viewer pipeline/supabase-nar-official.sql:363-364= 同じ表の作成文の別の写し(どちらで今の DB が作られたかは Fable が確認)③viewer horse_health_organizers_20260904.sql:137= support_condition_group の CHECK= **効かない**(support_source_kind が 4 主催者に限られる)④同:209= 表 nar_health_evidence の CHECK= **効かない**(stage が pre/in/post だけ・auction_disclosure が無い)⑤同:590= 主催者 RPC の門番= **効かない**(event_type に auction_disclosure が無い)⑥viewer horse_health_all_organizers_20260905.sql:130= support の CHECK= **効かない**(同上)⑦viewer ai_feat_local_schema.sql:172・189= 手元 Postgres の写し= text で CHECK 無し= **効かない**⑧★viewer js/data.js:4445 HEALTH_GROUPS= 画面が読む行の門番。使う所は 4648(getHealthEventsByName)・4711(健康の索引)・4870(馬ページのオークション)の 3 か所で、**一覧に無い値の行は黙って捨てられる**⑨★viewer js/pages/race.js:3116-3122 ANA_CAUSE= 画面の日本語名(3146 で hasOwnProperty= 無い値は cause 無し)⑩viewer public/js/data.js:4445・public/js/pages/race.js:3090-3095= **build.sh の出力で .gitignore 済み**= 直さない(作り直される)⑪viewer tests/horse_health_test.mjs:27,40,47,54,61,68,75,82,89,132,149・tests/health_index_test.mjs:27,38= 作り物の値(足すだけなら壊れないが HEALTH_GROUPS を変える回に新しい値の項を足す)⑫★nar-jobs cloud/horse_health.py:87-96 GROUP_RULES= **オークションの分類を決めている本体**(5 値・unspecified は無い)⑬★同:774= 「傷病歴の申告あり」を unspecified で入れる(オークション専用)⑭★同:319-320= 複数当たったとき "disease" を落とす(オークション・主催者の両方)⑮nar-jobs cloud/health_org.py:43 の注記と 230 の unspecified= 主催者経路= **効かない**⑯nar-jobs pipeline/sql/horse_health_20260903.sql:54-55・horse_health_all_organizers_20260905.sql:130= viewer の**部分写し**= DB には効かない⑰nar-jobs js/data.js:4987 HEALTH_GROUPS・5347 unspecified= nar-viewer から init で写したもの= **効かない**(nar-jobs は build.sh も index.html も無くサイトを作らない・py は匿名キーと VENUES しか読まない)⑱両リポの docs/*.md= 説明
通信: ±0(grep だけ・便も DB も触っていない)
回帰: 無し(コードもテストも 1 字も変えていない)
変えた点: 無し(a0 は調べるだけ)
⚠: **(1) health_org.py が horse_health.py の `_groups` を import している**(cloud/health_org.py:44)= b で「主催者の GROUP_RULES は触らない」を守るには、**新しい AUCTION_GROUP_RULES を作って `_auction_details` の中の `_groups(detail)` の呼び出しだけを差し替える**こと(`_groups` 本体を書き換えると主催者 4 場の分類まで変わる)。**(2) 画面の門番 HEALTH_GROUPS(viewer js/data.js:4445)は SQL と別に必ず当て直す**= SQL の CHECK だけ広げて js を忘れると、新しい分類の行は DB に入るのに**画面から黙って消える**(3 か所とも捨てる側)。**(3) nar-jobs の pipeline/sql と js/data.js は viewer の古い写し**= 今回は直さなくても本番に影響しないが、放っておくと次の人が写しを正本と間違える(そろえるか、写しに「正本は viewer」と 1 行入れるかは Fable 判断)。**(4) 今の DB に実際に効いている CHECK の字は、移行ファイル 2 本(①②)のどちらとも違う可能性がある**= a の SQL を書く前に Fable が本番の pg_constraint を 1 回見ること
要判断: なし(a0 は一覧を出すだけ。上の ⚠(3)(4) は a の前に Fable が決める)
