commit: 枝 s196b-fix(nar-jobs・origin/master 8394fba の上・設計 docs/opus_s196b3_hist_fix_20260917.md・⛔push/merge/deploy なし・⛔DB へは書いていない(--all の投入は Fable)・yml は触っていない)= **cloud/nankan_hist.py だけ(+6 行 −2 行)**: `run_points`= 着順が無い走で、注記に「失格」「降着」→ **0 点「失格=0」**(原文 ④ L234)/注記が無く、そのレースの nar_races.cancelled が空でない → **0 点「取り止め」**/それ以外は今のまま None「着順なし」。REST の nar_races の select に `cancelled` を足した(CSV は第 1 段の races.csv に列が既にある)。`--shard` の help の `%` を `%%` に(`--help` が落ちていた)。tests/test_nankan_hist.py に 2 項(取り止め= 0・cancelled が空なら従来どおり None/失格= 0)
検品: `unittest discover -s tests -p "test_nankan*.py"` **14 本 OK**・`--help` が出る。**合格= `--from-csv data/recon --all --out … --today 2026-09-17`(第 1 段の CSV・9/17 取得)で 決められない走 = {S格の重賞(pts なし) 341・遠征先の地方交流重賞 13}= 「着順なし」0(前 275)**。**points null の行 3,254 → 1,135**(行 45,499・馬 3,339・official 3,233・負 9 は前と同じ)。前後の CSV を突き合わせ= 行のキーは同じ・null → 値 2,119 行・既に値のあった行の値の変化 0・他の列(kaku_ran/kaku_src/src 等)の変化 0
通信: 0(CSV だけ。本番 DB・nankankeiba とも読んでいない)
回帰: hist_rows(official の起点・書き直さない規則)・kaku の規則・表の列は触っていない。日次(--days 3)は REST の select に列が 1 つ増えるだけ(本数は同じ)
変えた点: なし(設計どおり)。補足= CSV の着順なし走の注記は「出走取消 1,010・競走除外 571・競走中止 536・空 1,441」で、「失格」「降着」の注記は第 1 段の CSV に 0 件= 今回の 275 走はすべて「取り止め」で 0 点になった。cancelled の値は 'refund' 95 レース・'nosale' 12 レース(空でなければ取り止めとして扱う)
⚠: **本番の null は設計の「前 2,188 行」(本番 45,536 行)と CSV の 3,254 行で基準が違う**= CSV の実行は official の既存行(起点)を読まないので、null が古い開催まで伸びる。本番で --all を流し直したときの null の数は、この 1,135 より少なくなる見込み(残りは S 格重賞 341 走= 結果ページの段 --results-dates --results-kind 重賞 で埋まる・遠征先の地方交流重賞 13 走)
要判断: (1) 本番の履歴の流し直し(`nankan_hist.py --apply --all`)は Fable= 既にある official の行は書き直さず、null だった行が埋まる (2) 「降着」を 0 点にする規則は設計どおり入れたが、降着は通常は着順が付く(着順があれば今までどおり着順で数える)= 着順の無い降着が来たときだけ効く
