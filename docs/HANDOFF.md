commit: 枝 s195c-fetch-relay(nar-jobs・origin/master 00cbe1f の上・⛔push/merge/deploy なし・⛔便は 1 回も回していない・⛔DB に 1 行も書いていない・⛔鍵は repo に 1 字も無い)= 新規 **workers/fetch-relay/**(`wrangler.toml`= name `nar-fetch-relay`・`crons = ["0 21 * * *"]`(06:00 JST)・**`workers_dev = false` で route も *.workers.dev も作らない**/`src/index.js`= 一覧 `SOURCES`(今は `ooi_official` 1 本・ua `nar-jobs-health/1.0`・pages 8・大井 RSS `?s=出来事&paged=N`)→ 純関数 `targets()` が 8 本に展開 → **1 本ずつ 3 秒あけて** fetch(UA・Accept-Language ja)→ 純関数 `rawRow()` で 1 行に(**200 のときだけ本文**・**2MB 超は入れず status -1**・通信断は status 0)→ `nar_fetch_raw?on_conflict=source,key` へ **1 回の POST で 8 行 upsert**(`Prefer: resolution=merge-duplicates,return=minimal`)・鍵は `env.SUPABASE_URL`/`env.SUPABASE_SERVICE_KEY`・**`fetch` ハンドラは 404 だけ**・**ログは `source key status=… bytes=…` だけで URL を出さない**)・新規 tests/fetch_relay_test.mjs 6 項・`cloud/health_org_ooi.py` の入口を **表読み**に(新規 `RAW_TABLE`/`RAW_FRESH_DAYS=3`・純関数 `_utc()`・`fresh_pages(rows, now, fresh_days)`・`read_raw()`・`list_documents(fetch, since, until, rows=None)`。**§195b の `FEED_BASE_DEFAULT`/`feed_base()`/`feed_url()`/`OOI_FEED_BASE` と `_page()` は削除**)・tests/test_ooi_feed_base.py → **tests/test_ooi_raw_read.py** に当て直し(9 項)・`.gitignore` に `node_modules/` と `.wrangler/`
検品: `unittest discover -p "test_*.py"` **75/75 OK**(73 → 表読み 9 項に差し替えて +2)・`node tests/fetch_relay_test.mjs` **6 項 緑**・`node tests/ai_feat_workflow_test.mjs` 緑・py_compile 通過・git diff --check 通過・禁止語 0。**鎖を手元で 1 回通した(⛔DB には書かない・先方への要求は 1 本だけ)**= Worker の `targets()[0]` から URL を取り、同じ名乗りで 1 回引き(**status 200 / 15,657 B / application/rss+xml; charset=UTF-8**)、Worker の `rawRow` と同じ形の行を便の `list_documents` に食わせて **文書 4 件(9/3・9/4・9/14・9/15)/ 行 11 件**= **欠けていた 9/14・9/15 が読める**。同じ行の `fetched_at` を 4 日前にすると `⚠ 使えない頁 paged=1 4.0 日前(古い)` → `RuntimeError: nar_fetch_raw に使える頁がありません`= **便の段が赤くなる**ことも確認。Worker 側は偽の fetch で 8 本を回して確認(先方 8 本・UA は全部 `nar-jobs-health/1.0`・403 の頁は本文を入れない・表へは 1 回だけ POST・**ログに URL が 1 本も出ない**・鍵が無ければ表へ投げない)
通信: **±0**(便も Worker も回していない・DB 読み書き 0)。検品で先方へ出したのは **1 本**だけ。運用に入った後は **先方へ 1 日 8 本**(Worker の cron 1 回 × 8 頁・3 秒あけ)・便は先方へ **0 本**(REST で表を 1 回読むだけ= REST 要求は今より 1 本増えるだけ)
回帰: `parse`・`SECTIONS`・`_IDENT`・`_LEAD`・`_race_date`・`_pub_date` と `list_documents` の頁送りの中身(`seen`・窓の外は飛ばす・古い頁で break)は **1 字も変えていない**(tests 5/7 で実物の本文から同じ行が出ることを確認)。`health_org.py` 本体・`health_org_kochi.py`・`health_org_saga.py`・`horse-health.yml`・`nar-watchdog.yml`(§195b の job `ooi-official` はそのまま)・DB の器は無傷
変えた点: (1) **1 頁も使えないときは例外にした**(段が赤くなる)= 設計の「古い/無いは『取れず』に数える」は、`limiter.errors` が `health_org.py` の中にあって adapter から触れない。0 件で緑のまま 8 日気づかなかったのが §195 の失敗なので、**全滅だけは黙って緑にしない**ようにした(一部が古いだけなら `⚠ 使えない頁 …` を出して読めるぶんを読む)。⛔`health_org.py` は 1 字も触っていない。(2) `wrangler.toml` に **`workers_dev = false`** を足した= 設計の「inbound の route は作らない」を確実にする(既定のままだと `*.workers.dev` の公開 URL が生える)。(3) テストの名前を `test_ooi_feed_base.py` → **`test_ooi_raw_read.py`** に変えた(`feed_base` が無くなったので名前が嘘になる)。(4) `.gitignore` に `node_modules/` と `.wrangler/` を足した(ユーザーが `npx wrangler` を回すと出る)
⚠: **(1) §195b の中継 `/feed/ooi` は使わなくなった**= この枝を入れたら viewer 側の `functions/feed/ooi.js` と `_middleware` の素通し 1 行は**撤去してよい**(設計の順のとおり)。⛔今はまだ消していない(viewer の枝は別)。**(2) 表 `nar_fetch_raw` は anon から読めない**= service_role だけ。私は DB の中身を確かめられないので、Worker を 1 回動かした後の 8 行(status 200)は **健康便の手押しで「文書 ≥1・errors 0」を見て**判断してください。**(3) 3 日の線**= `RAW_FRESH_DAYS=3`。Worker が止まると 3 日後に便が赤くなる(その前に watchdog の `ooi-official` が 2 日で鳴る)。大井の非開催が 3 日続いても表は毎日更新されるので誤って鳴らない。**(4) `compatibility_date = "2026-09-16"`** にしてある= wrangler が古くて弾く場合はユーザー側で日付を下げてください。**(5) Worker のログは Cloudflare の画面でしか見えない**(便のログには出ない)= 8 本のうち何本取れたかは、表の `status` を service key で引くのが確実
要判断: なし(設計のとおり)。**ユーザーの手順は下の 4 行**
  1) `cd workers/fetch-relay && npx wrangler login`
  2) `npx wrangler secret put SUPABASE_URL`(本番の Supabase の URL を貼る)
  3) `npx wrangler secret put SUPABASE_SERVICE_KEY`(service_role の鍵を貼る・⛔anon では表に書けない)
  4) `npx wrangler deploy` → 初回だけ手押し= Cloudflare の画面 Workers → nar-fetch-relay → Settings → Trigger Events の Cron Triggers の「Run」(または手元で `npx wrangler dev --test-scheduled` → 別窓で `curl "http://localhost:8787/__scheduled?cron=0+21+*+*+*"`)
