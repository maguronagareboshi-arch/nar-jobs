commit: 枝 s200-calc-hist(nar-jobs・origin/master 768060e の上・設計 docs/opus_s200_calc_hist_20260917.md・⛔push/merge/deploy なし・⛔DB へは書いていない(投入は Fable)・yml は触っていない)= **cloud/class_calc.py に段を 1 つ(+142 行 −4 行)**: 引数 `--hist-days [N]`(値なし= 3)・`--hist-all`(`--since` 以降)・`--shard i/n`・`--out`・`--today`/`hist_targets`= 同じ馬・同じ場の地方の走の日 D ごとに 1 つ(中央・他場・窓の外は作らない)+ 出走予定(nar_runs の race_date ≥ きょう を場ごとに 1 本・同名の別馬は生年月日で除く)/`hist_calc`= apply の 1 頭ぶんと同じ手順で asof だけ D(kochi_calc/obi_calc/saga_calc(2歳は出さない)/tokai_calc/hyogo_calc をそのまま呼ぶ・高知の転入前の note は「D より前の最新の地方の走」で判定・兵庫の表示方針は「D より前の最新の走の格組」で判定)/`hist_stage`= `--apply` のときは各場の calc 列の書き込みが終わった後に upsert(on_conflict code,prefix,asof・500 行ずつ)・`--apply` なしで段だけ流すと dry-run(+`--out` で CSV)。5 つの main の apply の流れ(calc 列)は最後の `return 0` を段の呼び出しにしただけ。**tests/test_class_calc_hist.py 3 項(53 行)**
検品: `unittest discover -p "test_*.py"` **92 本 OK**・py_compile 通過・`--help` が出る。**合格 1= 高知 ピクラリーダ(30028402286・9/13 高知 取消の転入馬・calc 列が 9/13 の便のまま残っている唯一の馬)の `--hist-days 5` の D=9/13 の行が calc 列と全項目一致**(Ｃ3下・600,000・次 Ｃ3上 あと 1,401,000・転入前の note・window 2024-09-01〜)。加えて **今日の便の calc 列(asof 9/17)758 頭すべてと、同じ段の計算(asof=9/17)が全項目一致**。**合格 2= `--hist-all --since 2024-01-01 --out`: kochi 20,173 行・obihiro 23,020・saga 14,051・kasamatsu 13,822・nagoya 22,790・sonoda 25,081・himeji 4,464(計 123,401 行)・cls null は全 prefix で 0**(兵庫の hide 4,121 行= 表示方針どおり)。`--hist-days 3`(9/14〜)= obihiro 79・nagoya 630・sonoda 296・kochi 0・saga 0(開催なし)
通信: 本番 DB の REST の読みだけ(書き込み 0)。`--hist-all`= kochi 86 本 15 秒・obihiro 82 本 17 秒・saga 100 本 17 秒・tokai 197 本 39 秒・hyogo 177 本 36 秒(5 並列)。`--hist-days 3`= 26〜80 本(既存の calc の読みに +場の数 本= 出走予定)。本番投入の upsert= 123,401 行 ÷ 500 = 約 250 本(Fable)
回帰: 式・線の表(class.js)・lag・note の規則・calc 列の書き方は不変(今日の 758 頭一致で確認)。`--hist-*` を付けなければ段は動かない(既存の便の出力は同じ)。`--verify` は段より前で返る
変えた点: (1) **東海・兵庫の履歴の prefix は「その走の場」**(apply の calc 列は「最後に走った地方の場」)= 笠松と名古屋を行き来する馬で、同じ日の calc 列と prefix(=東海の数え方の区切り)が違いうる。(2) 高知の転入前の note・兵庫の表示方針は、きょうの台帳ではなく「D より前の走」で判定した(D の朝の便と同じになるように)。(3) 出走予定の日を取るのに、場ごとに nar_runs を 1 本足した(台帳の runs にはまだ無いため)。(4) `--today` を足した(検算用)
⚠: **生年月日の無い馬は、年齢を台帳の「いまの年齢」で代わりに数える(apply と同じ式)= 過去の D では 2歳/3歳の判定がずれうる**= 高知 63 頭・1,037 行/帯広 10 頭・353 行(佐賀・東海・兵庫は生年月日が無ければ行を作らない)。また既存の fetch_* の nar_runs の読みは order なしの offset ページング(既存のまま・馬名の集合を作るだけ)
要判断: (1) 本番投入= `class_calc.py --prefix <kochi|obihiro|saga|tokai|hyogo> --hist-all --since 2024-01-01 --apply --env …` を 5 本(Fable)。⚠`--apply` は calc 列も書き直す(今日の値で同じ内容)。calc 列を触らずに段だけ書く引数は無い (2) 朝の便には各場の行に `--hist-days 3` を足すだけ(Fable・yml) (3) 生年月日の無い馬の過去の行を出すか(上の ⚠)
