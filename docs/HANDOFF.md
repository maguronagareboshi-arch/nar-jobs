commit: e4fdca7..4dd2680(枝 s240・22 コミット= 土台 / 表ごと 5 本 / 検算と巻き戻し / 順序の入れ替え 6 本 / 本番の実物に合わせ 6 本・push なし)
検品: ⛔本番 DB には一切触っていない(SQL も REST も無し)。手元は `py -3.12 tests/test_partition_s240_sql.py` が ALL PASS(8 ファイル)= begin と commit の数・vacuum がトランザクションの外・$$ の閉じ・区画の範囲が隙間なく続く・attach の相手が _archive_part・差分+rename 2 本+notify が同じトランザクション・delete が rename より後・statement_timeout・notify pgrst。SQL は **2026-09-22 18:10 に検品役が読んだ本番の実物**に合わせてある(列・PK・check・索引・RLS/policy・grant・comment・trigger・外部キー・依存 view)。§0 は当てる直前の最終確認として残した。
通信: +0(表を増やしていない。読む先も今までと同じ 5 表)
回帰: SQL と覚書と検査だけ= 画面・便のコードは 1 行も触っていない。既存の読み手(js/data.js の約 35 本・Actions の \copy・nar_search_runs / _v2)は表名が変わらないので変更 0。
変えた点: `pipeline/sql/partition_s240/` に 8 本(00 控え / 10 votes / 20 payouts / 30 races / 40 runs / 50 run_facts / 98 巻き戻し / 99 検算)。各表 §0 実物の確認 → §1 親と区画 8(2030 まで)→ §2 年ごとの写し → **§4' 差分の取り直し+rename+notify を 1 トランザクション** → **§3' archive_part の delete と check と attach** → §5 grant と vacuum。⛔順序を入れ替えた理由= 旧い順では今年のデータが画面から 15〜25 分消える窓があった。今は一時的に見えないのは 2022-10 以前だけ。ほか `docs/notes_s240_migration.md`(夜に当てる順・所要・止めどころ)・`tests/test_partition_s240_sql.py`・設計書の写し。
⚠: 手元の git grep で「依存 view 0」と書いたのは**誤り**で、実物には `nar_sales_hourly`(nar_races 依存)があった。view は OID で表に結び付くので rename では付いてこない= `30_nar_races.sql` の §4' の中(rename の直後・同じトランザクション)で `create or replace view` して親に結び直す手順を足した(検算は 99_verify の 4-5)。ほかの実測との差分= `nar_runs_race_idx` は存在しない・`nar_runs (horse_name text_pattern_ops)` と `nar_races (race_date desc, track)` が手元に無かったので親に追加・grant は全権限・comment は nar_run_facts だけ・`nar_races` だけ policy 2 本(marks_writer)。詳細は notes_s240_migration.md §6。
要判断: ①当てる順と夜の時間割(全部で 2.5〜3 時間・小さい 3 表だけで止めてもよい)③`nar_run_facts` の読み手 0 の列を落とすかは未着手(ユーザー判断のまま)・区画は 2030 まで作ったので毎年足す作業は 2030 年まで不要 ④`docs/opus_rules.md` は写しただけで commit していない(公開リポに本体名・メールを書かない規則のため)。
