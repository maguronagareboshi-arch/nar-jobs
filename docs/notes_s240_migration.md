# §240 夜に当てる順・所要の見込み・止めどころ(2026-09-22 / Opus 実装が書いた下書き)

設計= `docs/proposal_s240_partition_20260922.md`。SQL= `pipeline/sql/partition_s240/`。
流すのは鍵を持つ担当(Fable / 検品役)。⛔Opus 実装は本番 DB に触っていない= 下の見込みは**計算**で、実測ではない。

## 0. 始める前(15 分)

1. ⛔便を全部止める: `.github/workflows/` の nar-refresh / nar-ai-feat / nar-ai-last / nar-watchdog /
   run-facts-backfill を無効化(または当夜の cron を飛ばす)・`rakuten_backfill`(§238b)を止める・
   本番の `pg_cron` の予定を確認。⛔他セッションの本番作業も止める。
2. `00_baseline_counts.sql` を流して **0-1 の 5 行を貼って控える**(これが無いと検算できない)。0-2 で
   「どの年も 25 万行未満」を確認。0-4 でディスクの空きを確認。
3. 各表の `§0` を流して、写しと本番の実物を答え合わせ。⛔1 つでも違ったら §1 の DDL を直してから進む。
   特に見るもの= 列の並び(0-1)・索引(0-2)・check(0-3)・policy(0-4)・trigger(0-6)・
   この表を指す外部キー(0-7)・この表に依存する view(0-9)。**0-6 / 0-7 / 0-9 が 0 行でなければ止めて相談**。

## 1. 当てる順(⛔小さい表から。1 表を §1〜§5 まで終わらせてから次へ)

| 順 | ファイル | 行(移す側) | 見込み | 止めどころ |
|---|---|---|---|---|
| 1 | `10_nar_race_votes.sql` | 約 6 万 | 5〜10 分 | 練習台。ここで手順が通らなければ**この夜は中止** |
| 2 | `20_nar_race_payouts.sql` | 約 6 万 | 5〜10 分 | 列の写しが推定= §0-1 で違ったら §1 を直す |
| 3 | `30_nar_races.sql` | 約 6 万 | 5〜10 分 | ここまでで 30 分。⛔画面(レース一覧)を 1 回見る |
| 4 | `40_nar_runs.sql` | 約 61 万 | 45〜70 分 | 本命。⛔ここで朝が近ければ 5 は翌夜へ回す |
| 5 | `50_nar_run_facts.sql` | 約 76 万 | 60〜90 分 | 派生表。落ちても画面は §238a2 前なので影響が小さい |

全部で **2.5〜3 時間**の見込み。⛔1〜3 だけで止めてもよい(表ごとに完結している)。

## 2. 各ステップの見込みと止めどころ(nar_runs を例に)

| 段 | 何をする | 見込み | ロック | 止めどころ |
|---|---|---|---|---|
| §1 | 親+区画 5 つ+索引 4 本+RLS/grant | 5 秒 | 新しい表だけ | 落ちたら `98_rollback.sql` 段 A(親を drop) |
| §2-1..6 | 年ごとに insert … select(13 万行×5) | 1 刻み 3〜8 分 / 合計 20〜35 分 | 旧表は読みだけ | 途中で止めてよい。続きは次の刻みから。落ちたら段 B |
| §2 最後 | 行数の突き合わせ | 10 秒 | — | ⛔合わなければ §3 へ進まない |
| §3-1 | 年ごとに delete(13 万行×5) | 1 刻み 1〜3 分 / 合計 10〜15 分 | 旧表に行ロック | ⛔ここから先は段 C(親から戻す)が要る。**朝が近いなら §3 に入らない** |
| §3-2 | `vacuum (analyze)` 旧表 | 5〜10 分 | 弱い | 止めても害はない(次の夜に流せる) |
| §3-3 | check を not valid | 数ミリ秒 | 短い排他 | — |
| §3-4 | validate constraint(約 68 万行の読み) | 1〜3 分 | 弱い(select は止まらない) | — |
| §3-5 | attach partition | 1〜5 秒 | 親に一瞬の排他 | ⛔索引の定義が違うとここで**索引を作り直し始める**(10 分以上)。長いときは中止して段 D |
| §4 | rename ×2 | 数ミリ秒 | 短い排他 | `lock_timeout 5s` で落ちたら数分後にもう一度。落ちても壊れない |
| §4 後 | `notify pgrst` | 即時 | — | ⛔ここで画面を 1 本開いて見る(レース画面) |
| §5 | 区画ごとの RLS/policy/grant | 5 秒 | 各区画に短い排他 | ⛔抜けると匿名キーで 0 行になる。`99_verify.sql` の 5) で必ず確認 |
| §5 後 | 区画ごとの `vacuum (analyze)` | 合計 10〜20 分 | 弱い | 朝までに終わらなくてよい(次の夜でも可) |

⛔**一番危ない窓**= §3-1 の delete を始めてから §4 の rename が終わるまで。この間は
「2022-11 以降の行が旧表に無く、親にしかない」= 旧表を見ている PostgREST から**今年のデータが消える**。
だから §2 の突き合わせが合ったら **§3-1 から §4 までは休まず続けて流す**(nar_runs で 15〜25 分)。

## 3. 1 表ごとの検算(⛔§4 の直後にその表の分だけ)

`99_verify.sql` の 1) 区画の木 → 2) 行数(控えと一致)→ 2-b 区画ごとの日付の幅 →
4-1/4-3 索引と policy → 5) 匿名キーで読める → 3) `explain` で区画の剪定。
- 合格= 行数一致・`race_date = '2026-09-22'` が **1 区画だけ**を読む・匿名キーで archive 側も読める。
- 馬名だけ(日付なし)は **6 区画とも読む**= 想定どおり(§238a2 で派生表の鍵へ寄せる)。

## 4. 全部終わったあと(朝までに)

1. 便を戻す → `nar-refresh` を 1 回手で回して成功を見る(⛔upsert が親で動くことの本当の確認)。
2. 画面 3 本(トップ / レース / 馬ページ)を自分の目で・console 0。
3. `docs/DESIGN.md` §10 に 1 行・WORKLOG に 1 行。
4. 翌日以降の宿題= ①`nar_runs_race_idx`(archive 区画に残った PK と同じ索引)を drop するか
   ②`*_archive_part` を別の tablespace に置くか(今は要らない)③2028 の区画を来年までに作る
   ⛔区画が無い日付の行を upsert すると**便が落ちる**= 毎年 12 月に 1 つ足す作業が増えた(台帳に書く)。

## 5. ⚠ 調べて分かった注意点(設計に足りていなかった所)

- **(a) PK に区画キー**= 5 表とも `race_date` が PK に入っている(nar_run_facts だけ PK の**先頭**)。
  unique 制約はほかに無い見込み(§0-3 で確認)。
- **(b) 旧表名に依存するもの**= git grep した結果、外部キー 0・trigger 0・view 0。
  `nar_search_runs` / `nar_search_runs_v2` は plpgsql の中で表名を書いているだけ= **実行時に名前で引く**ので
  rename の影響なし。`nar_sales_daily` view は `nar_sales`(対象外)の view。
  Actions の `\copy (select * from …)` と `js/data.js` の REST も表名が変わらないので変更 0。
- **(c) PostgREST の upsert**= PG17 の partitioned table は `on conflict (…) do update` が使える
  (競合を見る unique 索引が区画キーを含むとき)。5 表とも PK がそれを満たす= `on_conflict=` は今のまま。
- **(d) 索引の名前**= 旧表の索引名(`nar_runs_pkey` / `nar_runs_horse_idx` …)は rename しても残るので、
  親の索引は `<表>_p_<名前>`(例 `nar_runs_p_horse_idx`)で作る。PK 制約は create table が自動で
  `<表>_p_pkey` にする。rename 後も `_p_` が名前に残るが害はない。
  ⛔副作用= `search_runs_20260919.sql` を後で流し直しても、旧名の索引が archive 区画に残っているため
  `create index if not exists` は素通りする(親には作られない)。親の索引は `partition_s240/` が正。
- **(e) 順番**= votes → payouts → races → runs → run_facts。
- **(f) RLS は区画のポリシーも効く**= 区画に RLS を有効にしてポリシーを置かないと、親越しの select が
  0 行になり得る。だから §5 は「有効化+ポリシー+grant」を必ず一組で流す。
- **(g) 2028 の区画が無い**= 区画の無い日付を入れると upsert が落ちる。来年の 12 月までに足す(§4-4)。
- **(h) `nar_race_payouts` の create table は手元に無い**= 列は `load_nar_official.py` からの推定。
  §0-1 が正。⛔違ったら `20_…` の §1 を直してから流す。
